"use strict";

define(['angularAMD'], function (app) {
    app.factory('EvenementenFactory', function ($timeout, $http, $q) {

    });
    app.service('EvenementenServices', function ($timeout, $http, $q) {

    });
});
