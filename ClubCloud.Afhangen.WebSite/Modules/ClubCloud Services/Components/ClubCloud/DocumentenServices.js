"use strict";

define(['angularAMD'], function (app) {
    app.factory('DocumentenFactory', function ($timeout, $http, $q) {

    });
    app.service('DocumentenServices', function ($timeout, $http, $q) {

    });
});
