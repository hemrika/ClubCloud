define(['angularAMD'], function (app) {
    "use strict";
    app.factory('FooterFactory', function ($timeout, $http, $q) {

        var footer = {};
        return footer;
    });
    app.service('FooterServices', function ($timeout, $http, $q) {

    });
});