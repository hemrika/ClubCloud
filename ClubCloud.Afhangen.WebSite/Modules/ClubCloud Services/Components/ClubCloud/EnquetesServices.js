"use strict";

define(['angularAMD'], function (app) {
    app.factory('EnquetesFactory', function ($timeout, $http, $q) {

    });
    app.service('EnquetesServices', function ($timeout, $http, $q) {

    });
});