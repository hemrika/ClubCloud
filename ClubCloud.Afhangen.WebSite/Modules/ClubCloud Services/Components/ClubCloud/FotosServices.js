"use strict";

define(['angularAMD'], function (app) {
    app.factory('FotosFactory', function ($timeout, $http, $q) {

    });
    app.service('FotosServices', function ($timeout, $http, $q) {

    });
});
