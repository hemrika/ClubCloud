"use strict";

define(['angularAMD'], function (app) {
    app.factory('KalendersFactory', function ($timeout, $http, $q) {

    });
    app.service('KalendersServices', function ($timeout, $http, $q) {

    });
});